#ifndef _QTMAP_SAMPLE_WIN_H_
#define _QTMAP_SAMPLE_WIN_H_

// A window with a single label 
int sample_label(int argc, char* argv[]);
// A window containing a spin box and a slider which managed by a layout
int sample_layout(int argc, char* argv[]);
// A dialog
int sample_dialog(int argc, char* argv[]);
// A dialog from ui file
int sample_ui_dialog(int argc, char* argv[]);
// An extensible dialog
int sample_extensible_dialog(int argc, char* argv[]);

#endif  // _QTMAP_SAMPLE_WIN_H_
